services:

  - type: web

    name: scm-assistant-api

    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      uvicorn backend.api:app --host 0.0.0.0 --port $PORT

    plan: free

    envVars:
      - key: PYTHON_VERSION
        value: 3.11